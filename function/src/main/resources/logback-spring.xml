<configuration>
    <springProperty scope="context" name="projectId" source="spring.ai.vertex.ai.gemini.project-id" defaultValue=""/>
    <springProperty scope="context" name="functionName" source="spring.cloud.function.definition" defaultValue="receiptProcessingFunction"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
        <log>projects/${projectId:-${GOOGLE_CLOUD_PROJECT:-${GCP_PROJECT:-unknown-project}}}/logs/${functionName}</log>
        <flushLevel>WARN</flushLevel>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CLOUD"/>
    </root>

    <logger name="dev.pekelund.responsiveauth" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CLOUD"/>
    </logger>

    <logger name="org.springframework.ai" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CLOUD"/>
    </logger>
</configuration>
