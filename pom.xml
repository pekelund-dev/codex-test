<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.7</version>
        <relativePath/>
    </parent>

    <groupId>dev.pekelund</groupId>
    <artifactId>responsive-auth</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>ResponsiveAuth</name>
    <description>Responsive authentication sample with web UI and receipt processing service</description>

    <modules>
        <module>core</module>
        <module>receipt-parser</module>
    </modules>

    <profiles>
        <profile>
            <id>include-web</id>
            <modules>
                <module>web</module>
            </modules>
        </profile>
    </profiles>

    <properties>
        <java.version>21</java.version>
        <google-cloud-firestore.version>3.33.0</google-cloud-firestore.version>
        <google-cloud-storage.version>2.40.1</google-cloud-storage.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.pekelund</groupId>
                <artifactId>responsive-auth-core</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
                <inherited>false</inherited>
            </plugin>
        </plugins>
    </build>
</project>
